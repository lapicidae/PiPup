<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Navigation Rail -->
    <LinearLayout
        android:id="@+id/nav_rail"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:background="@color/colorSurfaceVariant"
        android:orientation="vertical"
        android:paddingTop="24dp"
        android:paddingBottom="24dp"
        android:paddingStart="24dp"
        android:paddingEnd="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginBottom="25dp"
            android:layout_marginEnd="16dp"
            android:src="@drawable/ic_banner_clear"
            app:tint="@color/colorPrimary"
            android:scaleType="fitStart"
            android:importantForAccessibility="no" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:scrollbars="none"
            android:requiresFadingEdge="vertical"
            android:fadingEdgeLength="40dp"
            android:clipToPadding="false">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <include android:id="@+id/nav_item_general" layout="@layout/nav_item" />
                <include android:id="@+id/nav_item_background" layout="@layout/nav_item" />
                <include android:id="@+id/nav_item_text_style" layout="@layout/nav_item" />
                <include android:id="@+id/nav_item_border" layout="@layout/nav_item" />
                <include android:id="@+id/nav_item_animation" layout="@layout/nav_item" />
                <include android:id="@+id/nav_item_updates" layout="@layout/nav_item" />
                <include android:id="@+id/nav_item_advanced" layout="@layout/nav_item" />

            </LinearLayout>
        </ScrollView>

    </LinearLayout>

    <!-- Submenu Content -->
    <ScrollView
        android:id="@+id/settings_scroll"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:fadeScrollbars="false"
        android:scrollbarStyle="outsideOverlay"
        android:scrollbars="vertical"
        android:descendantFocusability="afterDescendants"
        android:contentDescription="@string/settings_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/nav_rail">

        <LinearLayout
            android:id="@+id/submenu_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="48dp" />
    </ScrollView>

    <!-- Live Preview Container (Truly an overlay) -->
    <FrameLayout
        android:id="@+id/preview_area"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.98"
        android:layout_marginEnd="10dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
